- With svg component, we only define an svg file which include svg component inside
- After to can use svg component we can use ReactComponent

- That mean we only use normal svg tag, not need wrapper it inside any react component